# ---------------------------------------------------------------------------
# Python: use 3.10 (recommended). Face libraries have the best Windows wheel
# coverage on 3.10; 3.13/3.14 are risky for insightface/dlib/deepface.
# Create the venv with:  py -3.10 -m venv .venv
# ---------------------------------------------------------------------------

# --- Core (Phase 0-4): stdlib only + tests. No heavy deps needed for the seam.
pytest>=8.0

# --- Config
python-dotenv>=1.0        # optional; config.py has a fallback parser if absent

# --- Evidence stage (Phase 3-4)
imagehash>=4.3            # perceptual hash (pHash) that survives crop/recompress
Pillow>=10.0             # image IO for hashing

# --- Face stage (Phase 1): choose ONE stack at Phase 1 (see decisions list).
# Left commented until we pick, to keep the Phase-0 install clean & fast.
#   Option A (recommended, accurate):
# insightface>=0.7
# onnxruntime>=1.17
#   Option B (simpler API, painful build on Windows/new Python):
# face_recognition>=1.3
# dlib>=19.24

# --- Search stage (Phase 3): reverse image search client. TBD in Phase 3.
# requests>=2.31          # if we script HTTP against a RIS provider
# google-search-results   # SerpAPI client, IF we choose SerpAPI (paid -> ask first)

# --- Polish (Phase 5)
rich>=13.7                # staged CLI output, colored panels/tables

# --- EVM backend (teammate; not installed by default)
# web3>=6.0               # teammate adds when implementing EVMLedgerAdapter
